const songs = document.querySelectorAll("li")
for (const song of songs) {
    // song.setAttribute("class", "color")
    song.classList.add("color")
}

// const songs = document.getElementById("#songs")
// songs.setAttribute("class", "color")